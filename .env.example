# Environment Variables for QLCSV API
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git!

# JWT Secret Key (minimum 32 characters, use strong random string)
# Generate one: openssl rand -base64 32
JWT_SECRET_KEY=your-super-secret-jwt-key-minimum-32-characters-here

# Database URL (for Render.com or similar platforms)
# Format: postgres://username:password@host:port/database
DATABASE_URL=postgres://username:password@host:5432/database

# Or use individual connection string (for local development)
# ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=qlcsv;Username=qlcsv;Password=your_password
